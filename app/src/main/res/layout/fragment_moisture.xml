<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:plainpie="http://schemas.android.com/apk/res-auto"
    tools:context=".activity.fragment.MoistureFragment"
    app:theme="@style/AppThemeMoisture">
    <android.support.design.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/containMoisture">
        <android.support.v4.widget.NestedScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/scrollViewMoisture"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">
            <include layout="@layout/raw_data"/>
        </android.support.v4.widget.NestedScrollView>
    </android.support.design.widget.CoordinatorLayout>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/exceptionMoisture"
        android:textSize="@dimen/text_size"
        android:layout_centerInParent="true"
        android:text="@string/exception"
        android:clickable="true"
        android:padding="@dimen/padding_exception"
        android:visibility="gone"/>
    <ProgressBar
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="@dimen/progressBar_size"
        android:layout_height="@dimen/progressBar_size"
        android:id="@+id/progressBarMoisture"
        android:indeterminate="false"
        android:layout_centerInParent="true"
        android:visibility="gone"/>
</RelativeLayout>
